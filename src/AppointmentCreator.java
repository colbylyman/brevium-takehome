import Schemas.AppointmentInfo;
import Schemas.AppointmentInfoRequest;
import Schemas.AppointmentRequest;

import java.util.ArrayList;

public class AppointmentCreator {
    static AppointmentInfo FitAppointment(AppointmentRequest request, ArrayList<AppointmentInfo> allAppointments) {
        // TODO Find a place for the appointment with the correct doctor and return a AppointmentInfoRequest

        return null;
    }
}
